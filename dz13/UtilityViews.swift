//
//  UtilityViews.swift
//  dz13
//
//  Created by Parshkova Daria on 20.03.2023.
//
/*
import Foundation
import UIKit
class Settings :  UIView {
    let blackTextColor = UIButton()
    let blueTextColor = UIButton()
    let brownTextColor = UIButton()
    
    override init(frame: CGRect) {
        super.init(frame:frame)
        blackTextColor.frame = CGRect(x:20, y:20, width:40, height: 40)
        blackTextColor.setImage(.init(systemName: "circle.fill"), for: .normal)
        blackTextColor.tintColor = .black
        blackTextColor.addTarget(self, action: #selector(blackTextColorPress), for: .touchUpInside)
        
        blueTextColor.frame = CGRect(x:50, y:20, width:40, height: 40)
        blueTextColor.setImage(.init(systemName: "circle.fill"), for: .normal)
        blueTextColor.tintColor = .blue
        blueTextColor.addTarget(self, action: #selector(blueTextColorPress), for: .touchUpInside)
        
        brownTextColor.frame = CGRect(x:80, y:20, width:40, height: 40)
        brownTextColor.setImage(.init(systemName: "circle.fill"), for: .normal)
        brownTextColor.tintColor = .brown
        brownTextColor.addTarget(self, action: #selector(brownTextColorPress), for: .touchUpInside)
        
        
        self.addSubview(blackTextColor)
        self.addSubview(blueTextColor)
        self.addSubview(brownTextColor)

    }
    
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
    
    
  
}
*/
